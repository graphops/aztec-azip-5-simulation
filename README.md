# Aztec Prover Rewards Simulator

This repository publishes the simulator as a static GitHub Pages report generated from `index.ipynb` with Quarto.

## Local Development

Install dependencies:

```bash
uv sync
```

Open the notebook locally:

```bash
uv run jupyter lab
```

Render the static site locally after installing Quarto:

```bash
uv run quarto render
```
